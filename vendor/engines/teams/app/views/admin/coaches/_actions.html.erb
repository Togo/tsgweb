<ul>
  <% if Admin::CoachesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_coaches_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_coach_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::CoachesController.sortable? and Coach.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Coaches"),
                 admin_coaches_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Coaches"),
                 admin_coaches_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
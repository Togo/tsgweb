<section id="body_content" class="engine_index">  
		<% @teams.each do |team| %>
			<div id="engine_index_list_container">
			<%= image_fu team.photo, :medium %>
   <p><%= link_to team.name+" ("+team.league+") ", team_path(team) %></p>
</div>
    <% end %>

</section>


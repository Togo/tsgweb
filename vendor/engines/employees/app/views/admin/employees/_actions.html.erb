<ul>
  <% if Admin::EmployeesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_employees_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_employee_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::EmployeesController.sortable? and Employee.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Employees"),
                 admin_employees_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Employees"),
                 admin_employees_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>